# Firebase AI Configuration (uses Firebase project setup)
# No additional environment variables needed for Firebase AI
# Configuration is handled through firebase_options.dart

# Firebase Configuration (if needed beyond firebase_options.dart)
# Add any additional API keys here if required

# Environment
ENVIRONMENT=development
